apiVersion: v1
kind: ConfigMap
metadata:
  name: operation-phase-config
  namespace: green-devops
data:
  default.yaml: |
    environment: prod
    debug: false
    # Copy config/default.yaml content here for ConfigMap
  carbon_config.yaml: |
    # Copy config/carbon_config.yaml content here
  sla_config.yaml: |
    # Copy config/sla_config.yaml content here
  scaling_config.yaml: |
    # Copy config/scaling_config.yaml content here
  job_policies.yaml: |
    # Copy config/job_policies.yaml content here
