apiVersion: v1
kind: Service
metadata:
  name: {{ include "operation-phase.fullname" . }}
  labels:
    {{- include "operation-phase.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "operation-phase.selectorLabels" . | nindent 4 }}
